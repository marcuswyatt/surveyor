import React, { Fragment } from 'react';

const About = () => {
    return (
        <Fragment>
            <h1>Surveyor</h1>
            <p>App that supplies information to Surveyor's in regards to points on a plane. For example distance between points, etc.</p>
            <p>Version 1.0.0</p>
        </Fragment>
    );
};

export default About;